# secret-key-to-qr-code

encode-secret-key.sh - encodes a pgp secret key as a series of png images of QR codes

decode-secret-key.sh - decodes a series of QR codes (from a webcam) into a pgp key and imports it
